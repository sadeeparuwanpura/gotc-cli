{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "installCommand": "npm ci",

  "rewrites": [
    {
      "//": "STEP 1 — replace the host below with your API's public HTTPS origin, keeping the /api suffix. Proxying through this domain is what keeps the session cookie first-party; see DEPLOY.md.",
      "source": "/api/:path*",
      "destination": "https://REPLACE-WITH-YOUR-API-HOST.example.com/api/:path*"
    },
    {
      "//": "React Router owns every other path. Vercel checks the filesystem first, so real files under /assets are served directly and only unmatched routes fall through to the app shell.",
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],

  "headers": [
    {
      "//": "Vite fingerprints these filenames, so they can be cached forever.",
      "source": "/assets/(.*)",
      "headers": [{ "key": "Cache-Control", "value": "public, max-age=31536000, immutable" }]
    },
    {
      "//": "Never cache an API response at the edge — they are per-session and carry Set-Cookie.",
      "source": "/api/(.*)",
      "headers": [{ "key": "Cache-Control", "value": "no-store" }]
    },
    {
      "source": "/(.*)",
      "headers": [
        { "key": "X-Content-Type-Options", "value": "nosniff" },
        { "key": "X-Frame-Options", "value": "DENY" },
        { "key": "Referrer-Policy", "value": "strict-origin-when-cross-origin" },
        { "key": "Permissions-Policy", "value": "camera=(), microphone=(), geolocation=()" }
      ]
    }
  ]
}
